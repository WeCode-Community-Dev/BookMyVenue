"use client";

import BookingLayout from "@/features/bookings/bookingLayout";

const page = () => {
    return (
        <div>
            <BookingLayout/>
        </div>
    );
};

export default page;
