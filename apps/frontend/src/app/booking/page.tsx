"use client";

import BookingLayout from "@/features/booking/components/BookingLayout";

const page = () => {
    return (
        <div>
            <BookingLayout/>
        </div>
    );
};

export default page;
