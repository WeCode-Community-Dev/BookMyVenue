export const THEME = {
    DARK: "dark",
    LIGHT: "light",
};

export const LOCAL_STORAGE = {
    APP_THEME: "APP_THEME",
};

export enum LoginStatus {
    LOGIN = "login",
    OTP = "otp",
    SUCCESS = "success",
}
