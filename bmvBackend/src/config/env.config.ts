// ConfigModule is registered globally in AppModule via ConfigModule.forRoot({ isGlobal: true }).
// No custom wrapper needed — ConfigService can be injected anywhere without re-importing ConfigModule.
