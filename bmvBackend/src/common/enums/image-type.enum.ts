/**
 * Category of a venue image.
 * Used to enforce minimum required photo coverage per venue.
 */
export enum ImageType {
  COVER = 'COVER',
  ENTRANCE = 'ENTRANCE',
  HALL = 'HALL',
  PARKING = 'PARKING',
  OTHER = 'OTHER',
}
