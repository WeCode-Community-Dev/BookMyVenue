version: '3.8'

services:
  # Relational Database Engine with Spatial Extensions
  bmv-spatial-db:
    image: postgis/postgis:15-3.3
    environment:
      POSTGRES_USER: bmv_dev_user
      POSTGRES_PASSWORD: bmv_ssh
      POSTGRES_DB: bmv_db
    ports:
      - "5432:5432"
    networks:
      - bmv-mesh-network

  # Core UI Layout & Gateway Proxy Router
  frontend-gateway:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - venue-service
    networks:
      - bmv-mesh-network

  # Microservice 1: Proximity Feeds & Location Indexes
  venue-service:
    build: ./services/venue-service
    ports:
      - "5001:5001"
    environment:
      DATABASE_URL: postgres://bmv_dev_user:bmv_ssh@bmv_db:5432/bmv_db
    depends_on:
      - bmv_db
    networks:
      - bmv-mesh-network

  # Microservice 2: Availability Grids & Reservation States
  booking-service:
    build: ./services/booking-service
    ports:
      - "5002:5002"
    networks:
      - bmv-mesh-network

  # Microservice 3: Decoupled Authentication & Identity Tokens
  auth-service:
    build: ./services/auth-service
    ports:
      - "5003:5003"
    networks:
      - bmv-mesh-network

networks:
  bmv-mesh-network:
    driver: bridge
