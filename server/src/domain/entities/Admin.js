class Admin {
  constructor({
    fullName,
    email,
    password,
    role
    
  }) {
    this.fullName = fullName;
    this.email = email;
    this.password = password;
    
  }
}

export default Admin;